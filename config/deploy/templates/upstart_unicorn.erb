description "Rails Application"

start on runlevel [2345]

script
  APP_ROOT=<%= current_path %>
  PATH=<%= rbenv_path %>/shims:<%= rbenv_path %>/bin:$PATH

  cd $APP_ROOT
  bundle exec unicorn_rails -c $APP_ROOT/config/unicorn.rb -E <%= rails_env %> >> /tmp/upstart.log 2>&1
end script

respawn
